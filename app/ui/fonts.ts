import { Inter, Suez_One } from 'next/font/google';

export const inter = Inter({
  subsets: ['latin'],
  display: 'block',
});

export const suezOne = Suez_One({
  subsets: ['latin'],
  display: 'block',
  weight: '400',
});
